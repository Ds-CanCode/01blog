# Database
POSTGRES_DB=mydb
POSTGRES_USER=mfadil
POSTGRES_PASSWORD=opendata

# Spring Boot Backend (DB overrides)
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/${POSTGRES_DB}
SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}

# Hibernate/JPA
SPRING_JPA_HIBERNATE_DDL_AUTO=create-drop
SPRING_JPA_SHOW_SQL=true
SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect
SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=true

# Logging
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY=DEBUG

# Multipart Upload Limits
SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=100MB
SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=100MB

# Cloudinary
CLOUDINARY_CLOUD_NAME=db0smfwdd
CLOUDINARY_API_KEY=821256124929233
CLOUDINARY_API_SECRET=1Vb5EkM8ihHI-D71vB59Od0F4Fg

# JWT (placeholder; update if your app uses it, e.g., via app.jwt.secret in properties or code)
JWT_SECRET=your_super_secret_key_here

# Frontend (optional runtime env var example)
API_BASE_URL=http://localhost:8080